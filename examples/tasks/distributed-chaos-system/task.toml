schema_version = "1.1"

[task]
name = "harbor/distributed-chaos-system"
description = "Fix non-deterministic behavior in a distributed billing system with probabilistic commits and replay injection."
authors = [
  { name = "0x01f", email = "hhxlovety@gmail.com" }
]
keywords = ["python", "distributed", "determinism", "debugging"]

[metadata]
difficulty = "extreme"
category = "distributed-systems"
tags = ["python", "determinism", "mutation", "replay", "distributed"]

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 1200.0

[environment]
build_timeout_sec = 300.0
cpus = 4
memory_mb = 4096
storage_mb = 8192
gpus = 0
allow_internet = false
mcp_servers = []

[verifier.env]
[environment.env]
[solution.env]
