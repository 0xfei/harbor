schema_version = "1.1"

[task]
name = "harbor/clickhouse-mergetree-debug"
description = "Debug a production crash in ClickHouse MergeTree partition serialization"
authors = [
  { name = "0x01f", email = "hhxlovety@gmail.com" }
]
keywords = ["clickhouse", "mergetree", "crash", "debug", "production-bug"]

[metadata]
difficulty = "medium"
category = "debugging"
tags = ["clickhouse", "mergetree", "crash", "partition", "serialization"]
time_limit = "20 minutes"

[verifier]
type = "manual"
instructions = "Human reviewer checks if the model identifies the correct fix"

[agent]
timeout_sec = 180.0
max_attempts = 3

[environment]
build_timeout_sec = 60.0
cpus = 1
memory_mb = 1024
storage_mb = 1024
gpus = 0
allow_internet = false
mcp_servers = []

[verifier.env]
[environment.env]
[solution.env]
