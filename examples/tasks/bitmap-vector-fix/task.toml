schema_version = "1.1"

[task]
name = "harbor/bitmap-vector-fix"
description = "Fix bugs in a multi-dimensional hash-based filter (BitMapManager) implementation in C++."
authors = [
  { name = "0x01f", email = "hhxlovety@gmail.com" }
]
keywords = ["c++", "debugging", "segfault", "vector", "data-structures"]

[metadata]
difficulty = "medium"
category = "debugging"
tags = ["c++", "bug-fix", "vector", "size-t", "ub"]

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 900.0

[environment]
build_timeout_sec = 300.0
cpus = 2
memory_mb = 512
storage_mb = 2048
gpus = 0
allow_internet = false
mcp_servers = []

[verifier.env]
[environment.env]
[solution.env]
